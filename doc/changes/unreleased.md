# Unreleased

## Summary

This release updated dependency to `structlog` and centralizes the configuration of `structlog`.

The release also fixes a problem in the Lua dependencies by removing the version-pinning of `lua-cjson` in file `advanced-analytics-framework-0.1.0-1.rockspec`.

## Bugfixes

* #334: Fixed Lua dependencies

## Refactorings

* #323: Updated depdendency to `structlog`
